name: Deploy Frontend

on:
  push:
    branches:
      - main  # 当 push 到 main 分支时触发部署

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: 检出代码
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: 设置 Node.js 环境
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'  # 设置 Node.js 版本，可以根据项目需要修改

      # Step 3: 安装依赖
      - name: Install dependencies
        run: npm install

      # Step 4: 构建前端项目
      - name: Build frontend
        run: npm run build  # 使用 Vite 构建项目

      # Step 5: 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        run: |
          npm install gh-pages --save-dev  # 安装 gh-pages 作为 dev 依赖
          npm run deploy  # 使用 gh-pages 部署到 GitHub Pages
